const HeroTitle = () => {
	return (
		<span className="font-poppins max-w-xl text-5xl font-semibold text-white">
			INSURANCE YOU CAN COUNT ON AT EVERY STAGE
		</span>
	);
};

export default HeroTitle;
